# Path: C:/Users/valic/OneDrive/Documents/TMS/backend/dispatch/apps.py

from django.apps import AppConfig

class DispatchConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'backend.dispatch'  # Ensure this matches the backend structure

